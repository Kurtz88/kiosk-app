# 로컬·vercel dev: 기본값으로 data/kiosk.sqlite 사용 (설정 불필요)
# Vercel 프로덕션만 /tmp DB 사용 — 틀린정보 신고는 VPS npm start 권장

# DB 경로 강제 (선택)
# KIOSK_DB_FILE=c:\kiosk\data\kiosk.sqlite
# KIOSK_DATA_DIR=c:\kiosk\data

# 1 이면 /tmp DB (테스트용), 0 이면 data/kiosk.sqlite 강제
# KIOSK_USE_TMP_DB=0
