# npm start → data/kiosk.sqlite (틀린정보 신고·식당 DB 영구 저장)

# DB 경로 직접 지정 (선택)
# KIOSK_DB_FILE=c:\kiosk\data\kiosk.sqlite
# KIOSK_DATA_DIR=c:\kiosk\data

# 1 = /tmp DB (Vercel 프로덕션만 권장). 로컬에서는 0 또는 미설정.
# KIOSK_USE_TMP_DB=0
